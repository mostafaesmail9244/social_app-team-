import 'package:flutter/material.dart';

class EditeProflieBody extends StatelessWidget {
  const EditeProflieBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
