import 'package:flutter/material.dart';

import 'widgets/edite_proflie_body.dart';

class EditeProflieView extends StatelessWidget {
  const EditeProflieView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: EditeProflieBody(),
    );
  }
}
